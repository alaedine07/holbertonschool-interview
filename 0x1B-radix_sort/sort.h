#ifndef _SORT_H
#define _SORT_H

/* Libraries */
#include <stdio.h>
#include <stdlib.h>

/* Prototypes */
void print_array(const int *array, size_t size);
void radix_sort(int *array, size_t size);

#endif /* _SORT_H */
